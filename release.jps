#!groovy
pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                echo "Build ..."
            }
        }
        stage ('UAT') {
            when {
                expression {
                    return (params.Environment ==~ /^(?i)(QA|UAT|PROD)$/)
                }
            }
            steps {
                echo 'Deploy to UAT.'
            }
        }
    }
}